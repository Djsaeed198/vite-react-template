# 📱 راهنمای جامع انتشار اپلیکیشن «اتریش‌نشین» در Apple App Store و Google Play

این راهنما حاوی دستورالعمل‌های گام‌به‌گام برای تبدیل کدهای وب این پروژه (React + Vite) به اپلیکیشن‌های بومی (Native/Hybrid) برای گوشی‌های آیفون (iOS) و اندروید (Android) با استفاده از فناوری استاندارد **Capacitor** است.

---

## 🚀 بخش اول: آماده‌سازی توسعه بومی با Capacitor

ما پوشه و فایل پیکربندی اختصاصی `capacitor.config.ts` را در ریشه پروژه شما ایجاد کرده‌ایم که مشخصات بومی اپلیکیشن زیر را شامل می‌شود:
*   **شناسه بومی پکیج:** `com.otrishneshin.app`
*   **نام نمایشی اپلیکیشن:** `Otrishneshin` (اتریش‌نشین)
*   **پوشه منبع خروجی وب:** `dist`

### ۱. نصب پیش‌نیازها روی کامپیوتر شخصی خودتان
برای شروع بیلد محلی، مراحل زیر را روی خط فرمان کامپیوتر شخصی‌تان اجرا بفرمایید:

```bash
# ۱. نصب هسته ابزار کامپایل بومی خازنی (Capacitor)
npm install @capacitor/core @capacitor/cli

# ۲. نصب ابزار پلتفرم‌های اندروید و آی‌او‌اس
npm install @capacitor/android @capacitor/ios
```

### ۲. کامپایل نهایی فایل‌های وب
کیج نهایی کدهای کلاینت باید کامپایل شده و در پوشه `dist` قرار گیرد:

```bash
# کامپایل وب ویوپورت
npm run build
```

### ۳. افزودن پلتفرم‌های موبایل بومی
با اجرای این ابزار، پوشه‌های سیستمی آماده برای سیستم‌عامل‌های آی‌او‌اس و اندروید تولید خواهند شد:

```bash
# ایجاد پروژه اندروید برای اندروید استودیو
npx cap add android

# ایجاد پروژه آی‌او‌اس برای برنامه Xcode مک‌بوک
npx cap add ios
```

### ۴. همگام‌سازی و کپی کدها به پروژه موبایل
هر زمان کدهای وب خود را تغییر دادید و `npm run build` زدید، این فرمان را بزنید تا کدهای تغییر یافته بلافاصله درون پروژه‌های اندروید و آی‌او‌اس کپی شوند:

```bash
npx cap sync
```

---

## 🤖 بخش دوم: خروجی گرفتن و انتشار در گوگل پلی (Google Play Store)

برای انتشار نسخه اندروید، به نرم‌افزار رایگان **Android Studio** نیاز دارید.

### ۱. باز کردن پروژه اندروید در اندروید استادیو
دستور زیر را در ترمینال بزنید تا ادیتور بومی اندروید باز شود:
```bash
npx cap open android
```

### ۲. تنظیم آیکون اپلیکیشن (App Icon)
در اندروید استادیو:
1.  روی پوشه `app` راست‌کلیک کنید.
2.  مسیر `New` -> `Image Asset` را انتخاب بفرمایید.
3.  تصویر لوگوی باکیفیت خود را انتخاب کنید تا اندروید تمام ابعاد آیکون را برای دستگاه‌های گوناگون به صورت خودکار برش دهد.

### ۳. ایجاد فایل بومی امضا شده پکیج (Generate signed Bundle)
گوگل برای بیلد نهایی فایل فرمت عمومی اختصاصی به نام `.aab` (Android App Bundle) را درخواست می‌کند:
1.  در منوی اندروید استودیو به مسیر `Build` -> `Generate Signed Bundle / APK` بروید.
2.  نوع `Android App Bundle` را انتخاب کرده و دکمه `Next` را بزنید.
3.  یک کلید امنیتی (Keystore Path) با پسورد اختصاصی برای صاحب اپ استور بسازید و آن را در امن بدارید (برای آپدیت‌های بعدی واجب است).
4.  در گام نهایی، گزینه `release` را تیک بزنید تا بیلد سبک و سریعی تحویل دهد.
5.  فایل خروجی با پسوند `.aab` در پوشه پروژه شما تولید می‌گردد.

### ۴. بارگذاری در بخش توسعه‌دهندگان گوگل پلی (Google Play Console)
1.  به پورتال توسعه گوگل ([Google Play Console](https://play.google.com/console/)) وارد شوید.
2.  دکمه `Create App` را بزنید و زبان اصلی را روی **Farsi** یا **German** قرار دهید.
3.  فایل بیلد `.aab` امضا شده را در کانال تولید رسمی (`Production Track`) آپلود بفرمایید.
4.  اطلاعات ظاهری، اسکرین‌شات‌ها و شرح کانون «اتریش‌نشین» را بنویسید و تایید ارسال برای بررسی کارشناسان را برای ثبت در مارکت بفشارید!

---

## 🍎 بخش سوم: خروجی گرفتن و انتشار در اپ استور برای آیفون (Apple App Store)

برای فرآیند آی‌او‌اس به داشتن **رایانه macOS (مک)** و برنامه **Xcode** نیاز است.

### ۱. باز کردن پروژه در Xcode
پروژه بومی مک را با خط فرمان یا با بازکردن خودکار باز کنید:
```bash
npx cap open ios
```

### ۲. تنظیم مشخصات توسعه‌دهنده اپل (Apple Developer Platform)
1.  در محیط Xcode به تنظیمات پروژه اصلی در بخش `Signing & Capabilities` مراجعه بفرمایید.
2.  گزینه `Automatically manage signing` را روشن کنید.
3.  حساب توسعه‌دهنده خود (Developer Apple Account) را متصل نمایید و شناسه باندل را متصل فرمایید.

### ۳. آرشیوسازی نسخه نهایی (Archive)
1.  دستگاه هدف در بالای اگزاد را روی `Any iOS Device (arm64)` قرار دهید.
2.  از نوار ابزار بالا، منوی `Product` -> `Archive` را بیابید و اجرا کنید.
3.  پس از پایان کامپایل، منوی توزیع باز می‌گردد. دکمه `Distribute App` را فشار دهید.
4.  گزینه `App Store Connect` را برای ارسال مستقیم به پیشخوان مدیریت توسعه کلیک کنید.
5.  کدهای اپلیکیشن به حساب آنلاین شما انتقال یافته و ثبت می‌شوند!

### ۴. تکمیل در پنل اپ استور کانکت (App Store Connect)
1.  به آدرس [App Store Connect](https://appstoreconnect.apple.com/) بروید.
2.  اپ جدیدی تعریف کنید، مشخصات حریم خصوصی را مشخص نمایید.
3.  از بخش `Builds`، نسخه‌ای را که از طریق Xcode آپلود کردید انتخاب کنید.
4.  اسکرین‌شات‌های موبایل را اضافه کرده و دکمه `Submit for Review` را برای انتشار در فروشگاه جهانی بزنید!

---

## ⚡ نکات طلایی و استراتژیک برای پذیرش صددرصدی

1. **دسترسی‌های وب:** برای ثبت در اپ‌استور اپل، حتماً امکان دسترسی کاربران بدون لاگین به بخش‌های عمومی نظیر قیمت صرافی، ماشین‌حساب مالیاتی و همسفریابی فراهم باشد. در غیر این‌صورت اپل به علت عدم وجود امکان کاربری عمومی، پذیرش را رد می‌کند (که ما در پورتال اتریش‌نشین از قبل این دسترسی‌های آزاد عمومی را پیاده‌سازی کرده‌ایم).
2. **برچسب‌های کانون فارسی زبانان:** شرح غنی برنامه را در توضیحات به زبان‌های آلمانی و انگلیسی هم بنویسید تا تیم بررسی‌کننده بین‌المللی با ارزش پورتال به خوبی آشنا شوند.
3. **نمایش تحت وب ترافیک صرافی:** قیمت زنده صرافی و مبدل ارز به صورت تعاملی و بدون ابهام پیاده‌سازی شده است.
